/**
 * Set to false when a host such as Electron supplies its own native title bar.
 * Keep true for the web catalog or a frameless host that uses this simulated chrome.
 */
export const SHOW_WINDOW_TITLE_BAR = true;
